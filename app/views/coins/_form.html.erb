<%= form_with(model: coin) do |form| %>
  <% if coin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(coin.errors.count, "error") %> prohibited this coin from being saved:</h2>

      <ul>
        <% coin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description, t('desctiption'), style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :acronym,t('acronym'), style: "display: block" %>
    <%= form.text_field :acronym %>
  </div>

  <div class ="field">
    <%= form.label :url_image, t('url_image'), style: "display: block" %>
    <%= form.text_field :url_image %>
  </div>

  <div class="field">
    <%= form.label :mining_type_id, t('mining_type') ,style: "display: block" %>
    <%= form.select("mining_type_id", @mining_type_options, {include_blank: 'select...'} ) %>
  </div>

  <div class="actions">
    <%= form.submit t('crud.submit')%>
  </div>
<% end %>
